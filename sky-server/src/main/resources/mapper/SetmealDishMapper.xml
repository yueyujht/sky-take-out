<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">
    <select id="getSetmealIdByDishId" resultType="java.lang.Integer">
        select setmeal_id from setmeal_dish where dish_id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
             #{id}
        </foreach>
    </select>

    <insert id="insertBatch">
        insert into setmeal_dish
            (setmeal_id, dish_id, name, price, copies)
        values
            <foreach collection="setmealDishes" item="dish" separator=",">
                (#{dish.setmealId},#{dish.dishId},#{dish.name},#{dish.price},#{dish.copies})
            </foreach>
    </insert>

    <delete id="deleteBatchBySmId">
        delete from setmeal_dish where setmeal_dish.setmeal_id in
        <foreach collection="setmealIds" item="setmealId" open="(" close=")" separator=",">
            #{setmealId}
        </foreach>
    </delete>
</mapper>
