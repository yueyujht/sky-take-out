<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderMapper">

    <insert id="submitOrder" useGeneratedKeys="true" keyProperty="id">
        insert into sky_take_out.orders
            (status, user_id, address_book_id, order_time, pay_method, pay_status, amount, remark, phone, address, user_name, estimated_delivery_time,consignee, delivery_status, pack_amount, tableware_number, tableware_status)
        VALUES
            (#{status},#{userId},#{addressBookId},#{orderTime},#{payMethod},#{payStatus},#{amount},#{remark},#{phone},#{address},#{userName},#{estimatedDeliveryTime},#{consignee},#{deliveryStatus},#{packAmount},#{tablewareNumber},#{tablewareStatus})
    </insert>

    <update id="updateOrder">
        update sky_take_out.orders
        <set>
            <if test="number != null">number = #{number}</if>
        </set>
        where id = #{id}
    </update>

    <insert id="insertBatchOrderDetail">
        insert into sky_take_out.order_detail
            (name, image, order_id, dish_id, setmeal_id, dish_flavor, amount)
        VALUES
            <foreach collection="orderDetailList" item="orderDetail" separator=",">
                (#{orderDetail.name},#{orderDetail.image},#{orderDetail.orderId},#{orderDetail.dishId},#{orderDetail.setmealId},#{orderDetail.dishFlavor},#{orderDetail.amount})
            </foreach>
    </insert>
</mapper>